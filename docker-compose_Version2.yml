version: '3'
services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379
    volumes:
      - ./backend:/backend
    depends_on:
      - redis
  redis:
    image: redis:6-alpine
    ports:
      - "6379:6379"